import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    paddingTop: 60,
    paddingBottom: 20,
    alignItems: "center",
  },
  titleText: {
    fontFamily: "Poppins-Regular",
    fontWeight: "600",
    lineHeight: 27,
    letterSpacing: 0.03,
    fontSize: 18,
  },
  topTabs: { paddingHorizontal: 20 },
});

export default styles;
